'def prime([x(str)], [y(int)], [z(str)]):\
    return (if len(x)==len(y)==len(z): x : z = float(z)*y)'


def primary(x: [str], y: [int], z: [str]):
    temp = []
    if len(x) == len(y) == len(z):
        for i in range(len(x) + 1):
            temp.append = y[i] * float(z[i])

    return {x: temp}


print(primary(['miron'], [123], ['10.25']))
